// SUBSTITUÍDO — use AppController (include/AppController.hpp).
// Este arquivo existe apenas para não quebrar referências de git history.
// Não está mais sendo compilado nem incluído em nenhum target do Makefile.
